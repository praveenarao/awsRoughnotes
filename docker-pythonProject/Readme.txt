#Readme
Same as node.js project except python project doesnt have package.json file.
#same prepreq as node project listed
# same steps for python proect to build image and run (create) an image to create container
#validate output
curl http://localhost:port
or curl http://ec2publicip:port
or in browser, open above listed urls